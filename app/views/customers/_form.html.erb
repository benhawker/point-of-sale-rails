<%= form_for @customer do |f| %>
	<%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.submit "Create Customer", class: "btn btn-primary" %>
<% end %>