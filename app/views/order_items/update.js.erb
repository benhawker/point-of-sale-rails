//Update each order item quantity
$(".<%= @order_item.id %>_quantity").html("<%= @order_item.quantity %>");

//Update order total
$(".<%= @order.id %>_total").html("<%= @order.total %>");

//Update number of seperate order items in order
$(".<%= @order.id %>_order_items_count").html("<%= @order.order_items.count %>");