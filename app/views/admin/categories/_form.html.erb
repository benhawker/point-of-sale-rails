<%= form_for([:admin, @category]) do |f| %>
  <%= f.text_field :name, placeholder: Enter a Category Name, class: "form-control form-width" %>
  <%= f.submit submit_text, class: "btn btn-primary" %>
<% end %>